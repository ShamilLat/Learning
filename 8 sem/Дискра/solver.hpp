#include <vector>
#include <list>

using namespace std;

class ListSolver {

public:
    ListSolver(int n, int C) {
    }

    pair<double, vector<int>> solve(const vector<double> &price, const vector<double> &weight) {
        // return max_p: double, answer: vector<int>
    }

};